<h1>Add a review</h1>
<h2><%= @restaurant.name %></h2>

<%= simple_form_for [@restaurant, @review] do |review| %>
  <%= review.input :rating, collection: [0,1,2,3,4,5] %>
  <%= review.input :content %>
  <%= review.button :submit, class: 'btn btn-primary' %>
<% end %>
